import { StruckThroughDirective } from './struck-through.directive';

describe('StruckThroughDirective', () => {
  it('should create an instance', () => {
    const directive = new StruckThroughDirective();
    expect(directive).toBeTruthy();
  });
});
