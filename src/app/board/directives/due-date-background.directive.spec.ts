import { DueDateBackgroundDirective } from './due-date-background.directive';

describe('DueDateBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DueDateBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
